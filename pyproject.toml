[project]
name = "starlightos"
version = "0.1.0"
description = "A cybersecurity and code developement operating system"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flask>=3.1.3",
]
