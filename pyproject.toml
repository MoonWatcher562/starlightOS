[project]
name = "starlightos"
version = "0.1.0"
description = "A cybersecurity and code developement operating system"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.136.1",
    "pydantic>=2.13.4",
    "requests>=2.34.2",
    "uvicorn>=0.47.0",
]
